import React from 'react'
import { Button } from './buttons'
import { Input } from './input'


export const Components = () => {
    return (
        <div>
            <h2>Buttons from Components</h2>

            <Button />

            <h2>Inputs from compnents</h2>

            <Input />

        </div>
    )
}
